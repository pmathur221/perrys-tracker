import React from 'react';
import ReactDOM from 'react-dom/client';
import PerrysProductivityTracker from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PerrysProductivityTracker />
  </React.StrictMode>
);
